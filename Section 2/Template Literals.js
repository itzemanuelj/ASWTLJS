/// Template literals (Template strings)

/// Multi-line strings
let firstName = `justin`;

/// single line string

let lName = `justin
j`;

/// multi line string

let sentence = `helllo ${firstName}`;
console.log(sentence);

/// hello justin

/// assign userName to the string buddy5000

let userName = "buddy5000";

console.log(`${userName} is my username`);
